import 'package:file_picker/file_picker.dart';

/// VM/mobile stub: there is no DOM to probe durations against. The IO path
/// relies on the size guard in [MediaUploadService] instead.
Future<double?> probeWebVideoSeconds(PlatformFile file) async => null;
