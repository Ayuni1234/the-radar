# package:test configuration for The Radar.
tags:
  golden-capture:
    # Pixel goldens (test/goldens/*.png) are OS/renderer-dependent — they are
    # regenerated on a dev machine and excluded from CI runs via:
    #   flutter test --exclude-tags=golden-capture
    description: OS-dependent screenshot capture / golden generation tests.
